# Backlog

> Unstarted tasks waiting to be prioritized

---

### 🔄 Evaluate feasibility of edit mode on existing vocab pages
Determine how the current vocab/concept browsing UI can support an edit mode. Key questions: how to load all profile-defined properties (including empty ones), how to add/edit/delete concepts inline, and what data loading strategy to use — source TTL + background + profile, annotated TTL exports, or a data adapter pattern that mirrors the data-processing enrichment pipeline.

